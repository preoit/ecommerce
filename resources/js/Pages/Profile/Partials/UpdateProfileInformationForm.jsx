import { CheckCircle2, Mail, Phone, Save, UserRound } from 'lucide-react';
import { useForm, usePage } from '@inertiajs/react';
const input='mt-2 h-12 w-full rounded-xl border border-slate-300 bg-slate-50 px-4 text-sm text-slate-800 outline-none transition placeholder:text-slate-400 focus:border-violet-500 focus:bg-white focus:ring-4 focus:ring-violet-100';
export default function UpdateProfileInformation(){const user=usePage().props.auth.user;const {data,setData,patch,errors,processing,recentlySuccessful}=useForm({name:user.name,email:user.email||'',phone:user.phone||''});const submit=e=>{e.preventDefault();patch(route('profile.update'),{preserveScroll:true})};return <section><header className="flex items-start gap-4"><span className="grid size-12 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-violet-100 to-indigo-100 text-violet-700"><UserRound className="size-5"/></span><div><h2 className="text-xl font-black tracking-tight text-slate-950">Profile Information</h2><p className="mt-1 text-sm leading-6 text-slate-500">Update your name, phone number and optional email address.</p></div></header><form onSubmit={submit} className="mt-7"><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-bold text-slate-700"><span className="flex items-center gap-2"><UserRound className="size-4 text-violet-500"/>Full name</span><input value={data.name} onChange={e=>setData('name',e.target.value)} autoComplete="name" required className={input}/>{errors.name&&<small className="mt-1.5 block font-semibold text-rose-600">{errors.name}</small>}</label><label className="text-sm font-bold text-slate-700"><span className="flex items-center gap-2"><Phone className="size-4 text-violet-500"/>Phone number</span><input type="tel" value={data.phone} onChange={e=>setData('phone',e.target.value)} autoComplete="tel" required className={input}/>{errors.phone&&<small className="mt-1.5 block font-semibold text-rose-600">{errors.phone}</small>}</label><label className="text-sm font-bold text-slate-700 md:col-span-2"><span className="flex items-center gap-2"><Mail className="size-4 text-violet-500"/>Email address <span className="font-medium text-slate-400">(optional)</span></span><input type="email" value={data.email} onChange={e=>setData('email',e.target.value)} autoComplete="email" placeholder="you@example.com" className={input}/>{errors.email&&<small className="mt-1.5 block font-semibold text-rose-600">{errors.email}</small>}</label></div><div className="mt-7 flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 pt-5"><p className="text-xs leading-5 text-slate-400">Changing a verified phone or email will require verification again.</p><div className="flex items-center gap-3">{recentlySuccessful&&<span className="inline-flex items-center gap-1.5 text-sm font-bold text-emerald-600"><CheckCircle2 className="size-4"/>Saved</span>}<button disabled={processing} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-600 to-indigo-600 px-5 text-sm font-bold text-white shadow-lg shadow-violet-200 transition hover:-translate-y-0.5 hover:shadow-xl disabled:translate-y-0 disabled:opacity-50"><Save className="size-4"/>{processing?'Saving...':'Save changes'}</button></div></div></form></section>}