import { Head, Link } from '@inertiajs/react';
import { Activity, ArrowLeft, BadgeCheck, CalendarDays, Eye, Heart, Mail, MapPin, PackageCheck, ShieldQuestion, ShoppingBag, Star, UserRound, WalletCards } from 'lucide-react';
import { useState } from 'react';
import AdminLayout from '@/app/layouts/AdminLayout';
import Seo from '@/app/components/Seo';

const money=value=>`BDT ${Number(value||0).toLocaleString('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const initials=name=>name?.split(' ').slice(0,2).map(p=>p[0]).join('').toUpperCase()||'C';
const empty=(title,text)=><div className="grid min-h-52 place-items-center p-8 text-center"><div><Activity className="mx-auto size-9 text-slate-300"/><h3 className="mt-3 font-black">{title}</h3><p className="mt-1 text-sm text-slate-500">{text}</p></div></div>;
function Status({verified,label}){const Icon=verified?BadgeCheck:ShieldQuestion;return <span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-bold ${verified?'bg-emerald-50 text-emerald-700 ring-1 ring-emerald-200':'bg-amber-50 text-amber-700 ring-1 ring-amber-200'}`}><Icon className="size-4"/>{label} {verified?'verified':'not verified'}</span>}
function MiniStat({icon:Icon,label,value,tone}){return <div className="flex min-w-0 items-center gap-2.5 rounded-xl border border-white/80 bg-white/80 px-3 py-2.5 shadow-[0_5px_16px_rgba(15,23,42,.05)] backdrop-blur"><span className={`grid size-8 shrink-0 place-items-center rounded-lg ${tone}`}><Icon className="size-4"/></span><div className="min-w-0"><p className="truncate text-[10px] font-bold uppercase tracking-wide text-slate-400">{label}</p><p className="truncate text-sm font-black text-slate-900">{value}</p></div></div>}
function ProductActivity({items,icon:Icon,emptyTitle}){return items.length?<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{items.map(item=><Link key={item.id} href={route('storefront.products.show',item.slug)} className="flex gap-3 rounded-xl border bg-white p-3 transition hover:border-violet-300 hover:shadow-sm">{item.image?<img src={item.image} alt="" className="size-14 rounded-lg bg-slate-50 object-contain"/>:<span className="grid size-14 place-items-center rounded-lg bg-violet-50 text-violet-500"><Icon className="size-5"/></span>}<div className="min-w-0"><b className="line-clamp-2 text-sm">{item.product}</b><p className="mt-1 text-xs text-slate-400">{item.date}</p></div></Link>)}</div>:empty(emptyTitle,'Customer activity will appear here when available.')}
export default function Show({customer,stats,orders,addresses,reviews,wishlist,recentlyViewed}){const[tab,setTab]=useState('overview');const tabs=[['overview','Overview'],['orders',`Orders (${stats.orders})`],['addresses',`Addresses (${stats.addresses})`],['reviews',`Reviews (${stats.reviews})`],['activity','Activity']];return <AdminLayout><Head title={customer.name}/><Seo title={`${customer.name} customer profile`} description="Customer account and purchase activity."/><div className="space-y-6"><Link href={route('customers.index')} className="inline-flex items-center gap-2 text-sm font-bold text-violet-700 hover:underline"><ArrowLeft className="size-4"/>Back to customers</Link><section className="overflow-hidden rounded-3xl border border-slate-200/80 bg-white shadow-[0_14px_38px_rgba(15,23,42,.065)]"><div className="relative border-b border-slate-100 bg-gradient-to-r from-white via-violet-50/60 to-indigo-50/70 px-5 py-5 sm:px-6"><div className="absolute right-8 top-0 h-full w-48 bg-[radial-gradient(circle_at_center,rgba(124,58,237,.09),transparent_68%)]"/><div className="relative grid gap-4 lg:grid-cols-[minmax(260px,.8fr)_minmax(520px,1.4fr)] lg:items-center"><div className="flex min-w-0 items-center gap-4"><span className="grid size-16 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-indigo-600 text-xl font-black text-white shadow-[0_10px_24px_rgba(109,40,217,.24)] ring-4 ring-white">{initials(customer.name)}</span><div className="min-w-0"><p className="text-xs font-extrabold uppercase tracking-[.16em] text-violet-600">Customer #{customer.id}</p><h1 className="mt-1.5 truncate text-xl font-black tracking-tight text-slate-950 sm:text-2xl">{customer.name}</h1><p className="mt-1 text-sm font-medium text-slate-500">Member since {customer.registeredAt}</p></div></div><div className="grid grid-cols-2 gap-2 xl:grid-cols-4"><MiniStat icon={ShoppingBag} label="Orders" value={stats.orders} tone="bg-violet-50 text-violet-700"/><MiniStat icon={PackageCheck} label="Completed" value={stats.completed} tone="bg-emerald-50 text-emerald-700"/><MiniStat icon={WalletCards} label="Spent" value={money(stats.spent)} tone="bg-blue-50 text-blue-700"/><MiniStat icon={MapPin} label="Addresses" value={stats.addresses} tone="bg-amber-50 text-amber-700"/></div></div></div><div className="grid divide-y divide-slate-100 lg:grid-cols-[1fr_1fr_2.2fr] lg:divide-x lg:divide-y-0"><div className="flex min-w-0 items-center gap-3 px-5 py-4 sm:px-6"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-violet-50 text-violet-600"><Mail className="size-5"/></span><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Email address</p><div className="mt-0.5 flex items-center gap-2"><p className="min-w-0 truncate text-sm font-bold text-slate-800">{customer.email||'Not added'}</p><span title={customer.emailVerified?'Email verified':'Email not verified'} className={`grid size-5 shrink-0 place-items-center rounded-full ${customer.emailVerified?'bg-emerald-50 text-emerald-600 ring-1 ring-emerald-200':'bg-amber-50 text-amber-600 ring-1 ring-amber-200'}`}>{customer.emailVerified?<BadgeCheck className="size-3"/>:<ShieldQuestion className="size-3"/>}</span></div></div></div><div className="flex items-center gap-3 px-5 py-4 sm:px-6"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-blue-600"><UserRound className="size-5"/></span><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Phone number</p><div className="mt-0.5 flex items-center gap-2"><p className="text-sm font-bold text-slate-800">{customer.phone||'Not added'}</p><span title={customer.phoneVerified?'Phone verified':'Phone not verified'} className={`grid size-5 shrink-0 place-items-center rounded-full ${customer.phoneVerified?'bg-emerald-50 text-emerald-600 ring-1 ring-emerald-200':'bg-amber-50 text-amber-600 ring-1 ring-amber-200'}`}>{customer.phoneVerified?<BadgeCheck className="size-3"/>:<ShieldQuestion className="size-3"/>}</span></div></div></div><div className="grid grid-cols-2 gap-x-5 gap-y-3 px-5 py-4 sm:grid-cols-3 sm:px-6">{[['Created',customer.registeredAt],['Last updated',customer.updatedAt],['Cancelled',stats.cancelled],['Reviews',stats.reviews],['Wishlist',stats.wishlist],['Recently viewed',stats.recentlyViewed]].map(([key,value])=><div key={key} className="min-w-0"><p className="text-[11px] font-bold uppercase tracking-wide text-slate-400">{key}</p><p className="mt-1 truncate text-sm font-extrabold text-slate-800">{value}</p></div>)}</div></div></section><section className="overflow-hidden rounded-3xl border border-slate-200/80 bg-white shadow-[0_12px_35px_rgba(15,23,42,.06)]"><nav className="flex overflow-x-auto border-b border-slate-200 bg-slate-50/80 px-3 sm:px-5" aria-label="Customer activity tabs">{tabs.map(([key,label])=><button key={key} onClick={()=>setTab(key)} className={`whitespace-nowrap border-b-2 px-4 py-4 text-sm font-bold ${tab===key?'border-violet-600 text-violet-700':'border-transparent text-slate-500 hover:text-slate-800'}`}>{label}</button>)}</nav><div className="p-5 sm:p-6">{tab==='overview'&&<div><div className="mb-5"><p className="text-xs font-extrabold uppercase tracking-[.14em] text-violet-600">Account status</p><h2 className="mt-1 text-xl font-black text-slate-950">Verification & protection</h2><p className="mt-1 text-sm text-slate-500">A quick view of this customer account's trust and deletion status.</p></div><div className="grid gap-4 lg:grid-cols-3"><div className={`rounded-2xl border p-5 ${customer.emailVerified?'border-emerald-200 bg-emerald-50/70':'border-amber-200 bg-amber-50/70'}`}><Status verified={customer.emailVerified} label="Email"/><p className="mt-3 text-sm leading-6 text-slate-600">{customer.emailVerified?'The email address has been verified.':'The customer has not verified an email address yet.'}</p></div><div className={`rounded-2xl border p-5 ${customer.phoneVerified?'border-emerald-200 bg-emerald-50/70':'border-amber-200 bg-amber-50/70'}`}><Status verified={customer.phoneVerified} label="Phone"/><p className="mt-3 text-sm leading-6 text-slate-600">{customer.phoneVerified?'The phone number has been verified.':'The customer has not verified the phone number yet.'}</p></div><div className={`rounded-2xl border p-5 ${customer.canDelete?'border-amber-200 bg-amber-50/70':'border-emerald-200 bg-emerald-50/70'}`}><p className={`font-extrabold ${customer.canDelete?'text-amber-800':'text-emerald-800'}`}>{customer.canDelete?'Eligible for deletion':'Purchase history protected'}</p><p className="mt-2 text-sm leading-6 text-slate-600">{customer.canDelete?'This unverified account has no purchase history and may be deleted.':'Verified accounts and accounts with order history cannot be deleted.'}</p></div></div></div>}{tab==='orders'&&(orders.length?<div className="overflow-x-auto"><table className="w-full min-w-[720px]"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-3">Order</th><th className="p-3">Date</th><th className="p-3">Payment</th><th className="p-3">Status</th><th className="p-3 text-right">Total</th><th/></tr></thead><tbody className="divide-y">{orders.map(order=><tr key={order.id}><td className="p-3 font-bold text-violet-700">{order.number}</td><td className="p-3 text-sm text-slate-500">{order.date}</td><td className="p-3 text-sm capitalize">{order.paymentStatus}</td><td className="p-3"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold capitalize">{order.status.replaceAll('_',' ')}</span></td><td className="p-3 text-right font-bold">{money(order.total)}</td><td className="p-3"><Link href={route('orders.show',order.id)} className="grid size-8 place-items-center rounded-lg bg-blue-50 text-blue-600"><Eye className="size-4"/></Link></td></tr>)}</tbody></table></div>:empty('No orders yet','This customer has not placed an order.'))}{tab==='addresses'&&(addresses.length?<div className="grid gap-4 md:grid-cols-2">{addresses.map(address=><article key={address.id} className="rounded-xl border p-4"><div className="flex justify-between"><b>{address.label}</b>{address.isDefault&&<span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">Default</span>}</div><p className="mt-3 text-sm font-semibold">{address.recipient} · {address.phone}</p><p className="mt-2 text-sm leading-6 text-slate-500">{address.address}{address.area?`, ${address.area}`:''}, {address.city}, {address.district}{address.postalCode?` - ${address.postalCode}`:''}</p></article>)}</div>:empty('No saved addresses','The customer has not saved a delivery address.'))}{tab==='reviews'&&(reviews.length?<div className="space-y-3">{reviews.map(review=><article key={review.id} className="rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-2"><Link href={route('storefront.products.show',review.slug)} className="font-bold text-violet-700">{review.product}</Link><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold capitalize">{review.status}</span></div><div className="mt-2 flex text-amber-400">{Array.from({length:5},(_,i)=><Star key={i} className={`size-4 ${i<review.rating?'fill-current':''}`}/>)}</div>{review.title&&<b className="mt-2 block text-sm">{review.title}</b>}<p className="mt-1 text-sm leading-6 text-slate-600">{review.description}</p><p className="mt-2 text-xs text-slate-400">{review.date}</p></article>)}</div>:empty('No reviews','This customer has not reviewed any product.'))}{tab==='activity'&&<div className="space-y-8"><section><h2 className="flex items-center gap-2 font-black"><Heart className="size-4 text-rose-500"/>Wishlist ({stats.wishlist})</h2><div className="mt-4"><ProductActivity items={wishlist} icon={Heart} emptyTitle="Wishlist is empty"/></div></section><section><h2 className="flex items-center gap-2 font-black"><Eye className="size-4 text-blue-500"/>Recently viewed ({stats.recentlyViewed})</h2><div className="mt-4"><ProductActivity items={recentlyViewed} icon={Eye} emptyTitle="No recently viewed products"/></div></section></div>}</div></section></div></AdminLayout>}