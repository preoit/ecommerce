import { Head, Link } from '@inertiajs/react';
import { Activity, ArrowLeft, BadgeCheck, CalendarDays, Eye, Heart, Mail, MapPin, PackageCheck, ShieldQuestion, ShoppingBag, Star, UserRound, WalletCards } from 'lucide-react';
import { useState } from 'react';
import AdminLayout from '@/app/layouts/AdminLayout';
import Seo from '@/app/components/Seo';

const money=value=>`BDT ${Number(value||0).toLocaleString('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const initials=name=>name?.split(' ').slice(0,2).map(p=>p[0]).join('').toUpperCase()||'C';
const empty=(title,text)=><div className="grid min-h-52 place-items-center p-8 text-center"><div><Activity className="mx-auto size-9 text-slate-300"/><h3 className="mt-3 font-black">{title}</h3><p className="mt-1 text-sm text-slate-500">{text}</p></div></div>;
function Status({verified,label}){const Icon=verified?BadgeCheck:ShieldQuestion;return <span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-bold ${verified?'bg-emerald-50 text-emerald-700 ring-1 ring-emerald-200':'bg-amber-50 text-amber-700 ring-1 ring-amber-200'}`}><Icon className="size-4"/>{label} {verified?'verified':'not verified'}</span>}
function Stat({icon:Icon,label,value,tone}){return <article className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 text-2xl font-black">{value}</p></div><span className={`grid size-11 place-items-center rounded-xl ${tone}`}><Icon className="size-5"/></span></div></article>}
function ProductActivity({items,icon:Icon,emptyTitle}){return items.length?<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{items.map(item=><Link key={item.id} href={route('storefront.products.show',item.slug)} className="flex gap-3 rounded-xl border bg-white p-3 transition hover:border-violet-300 hover:shadow-sm">{item.image?<img src={item.image} alt="" className="size-14 rounded-lg bg-slate-50 object-contain"/>:<span className="grid size-14 place-items-center rounded-lg bg-violet-50 text-violet-500"><Icon className="size-5"/></span>}<div className="min-w-0"><b className="line-clamp-2 text-sm">{item.product}</b><p className="mt-1 text-xs text-slate-400">{item.date}</p></div></Link>)}</div>:empty(emptyTitle,'Customer activity will appear here when available.')}
export default function Show({customer,stats,orders,addresses,reviews,wishlist,recentlyViewed}){const[tab,setTab]=useState('overview');const tabs=[['overview','Overview'],['orders',`Orders (${stats.orders})`],['addresses',`Addresses (${stats.addresses})`],['reviews',`Reviews (${stats.reviews})`],['activity','Activity']];return <AdminLayout><Head title={customer.name}/><Seo title={`${customer.name} customer profile`} description="Customer account and purchase activity."/><div className="space-y-6"><Link href={route('customers.index')} className="inline-flex items-center gap-2 text-sm font-bold text-violet-700 hover:underline"><ArrowLeft className="size-4"/>Back to customers</Link><section className="overflow-hidden rounded-2xl border bg-white shadow-[0_12px_32px_rgba(30,41,59,.07)]"><div className="h-24 bg-gradient-to-r from-slate-950 via-violet-950 to-indigo-900"/><div className="px-5 pb-6 sm:px-7"><div className="-mt-10 flex flex-col gap-4 sm:flex-row sm:items-end"><span className="grid size-20 shrink-0 place-items-center rounded-2xl border-4 border-white bg-gradient-to-br from-violet-500 to-indigo-600 text-2xl font-black text-white shadow-lg">{initials(customer.name)}</span><div className="min-w-0 flex-1 pb-1"><p className="text-xs font-bold uppercase tracking-wider text-violet-600">Customer #{customer.id}</p><h1 className="mt-1 text-2xl font-black">{customer.name}</h1><p className="text-sm text-slate-500">Registered {customer.registeredAt}</p></div><div className="flex flex-wrap gap-2 pb-1"><Status verified={customer.emailVerified} label="Email"/><Status verified={customer.phoneVerified} label="Phone"/></div></div><div className="mt-5 grid gap-3 rounded-xl bg-slate-50 p-4 sm:grid-cols-2"><p className="flex items-center gap-2 text-sm"><Mail className="size-4 text-violet-500"/><b>Email:</b><span className="truncate text-slate-600">{customer.email||'Not added'}</span></p><p className="flex items-center gap-2 text-sm"><UserRound className="size-4 text-violet-500"/><b>Phone:</b><span className="text-slate-600">{customer.phone||'Not added'}</span></p></div></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={ShoppingBag} label="Total orders" value={stats.orders} tone="bg-violet-50 text-violet-700"/><Stat icon={PackageCheck} label="Completed" value={stats.completed} tone="bg-emerald-50 text-emerald-700"/><Stat icon={WalletCards} label="Total spent" value={money(stats.spent)} tone="bg-blue-50 text-blue-700"/><Stat icon={MapPin} label="Saved addresses" value={stats.addresses} tone="bg-amber-50 text-amber-700"/></section><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><nav className="flex overflow-x-auto border-b bg-slate-50 px-3" aria-label="Customer activity tabs">{tabs.map(([key,label])=><button key={key} onClick={()=>setTab(key)} className={`whitespace-nowrap border-b-2 px-4 py-4 text-sm font-bold ${tab===key?'border-violet-600 text-violet-700':'border-transparent text-slate-500 hover:text-slate-800'}`}>{label}</button>)}</nav><div className="p-5 sm:p-6">{tab==='overview'&&<div className="grid gap-6 lg:grid-cols-2"><div><h2 className="font-black">Account overview</h2><dl className="mt-4 divide-y rounded-xl border">{[['Account created',customer.registeredAt],['Profile last updated',customer.updatedAt],['Cancelled orders',stats.cancelled],['Product reviews',stats.reviews],['Wishlist products',stats.wishlist],['Recently viewed',stats.recentlyViewed]].map(([key,value])=><div key={key} className="flex justify-between gap-4 px-4 py-3 text-sm"><dt className="text-slate-500">{key}</dt><dd className="text-right font-bold">{value}</dd></div>)}</dl></div><div><h2 className="font-black">Account protection</h2><div className="mt-4 space-y-3"><div className={`rounded-xl border p-4 ${customer.canDelete?'border-amber-200 bg-amber-50':'border-emerald-200 bg-emerald-50'}`}><b className={customer.canDelete?'text-amber-800':'text-emerald-800'}>{customer.canDelete?'Eligible for deletion':'Protected account'}</b><p className="mt-1 text-sm leading-6 text-slate-600">{customer.canDelete?'This account is unverified and has no purchase history.':'Verified accounts and accounts with any order history cannot be deleted.'}</p></div></div></div></div>}{tab==='orders'&&(orders.length?<div className="overflow-x-auto"><table className="w-full min-w-[720px]"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-3">Order</th><th className="p-3">Date</th><th className="p-3">Payment</th><th className="p-3">Status</th><th className="p-3 text-right">Total</th><th/></tr></thead><tbody className="divide-y">{orders.map(order=><tr key={order.id}><td className="p-3 font-bold text-violet-700">{order.number}</td><td className="p-3 text-sm text-slate-500">{order.date}</td><td className="p-3 text-sm capitalize">{order.paymentStatus}</td><td className="p-3"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold capitalize">{order.status.replaceAll('_',' ')}</span></td><td className="p-3 text-right font-bold">{money(order.total)}</td><td className="p-3"><Link href={route('orders.show',order.id)} className="grid size-8 place-items-center rounded-lg bg-blue-50 text-blue-600"><Eye className="size-4"/></Link></td></tr>)}</tbody></table></div>:empty('No orders yet','This customer has not placed an order.'))}{tab==='addresses'&&(addresses.length?<div className="grid gap-4 md:grid-cols-2">{addresses.map(address=><article key={address.id} className="rounded-xl border p-4"><div className="flex justify-between"><b>{address.label}</b>{address.isDefault&&<span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">Default</span>}</div><p className="mt-3 text-sm font-semibold">{address.recipient} · {address.phone}</p><p className="mt-2 text-sm leading-6 text-slate-500">{address.address}{address.area?`, ${address.area}`:''}, {address.city}, {address.district}{address.postalCode?` - ${address.postalCode}`:''}</p></article>)}</div>:empty('No saved addresses','The customer has not saved a delivery address.'))}{tab==='reviews'&&(reviews.length?<div className="space-y-3">{reviews.map(review=><article key={review.id} className="rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-2"><Link href={route('storefront.products.show',review.slug)} className="font-bold text-violet-700">{review.product}</Link><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold capitalize">{review.status}</span></div><div className="mt-2 flex text-amber-400">{Array.from({length:5},(_,i)=><Star key={i} className={`size-4 ${i<review.rating?'fill-current':''}`}/>)}</div>{review.title&&<b className="mt-2 block text-sm">{review.title}</b>}<p className="mt-1 text-sm leading-6 text-slate-600">{review.description}</p><p className="mt-2 text-xs text-slate-400">{review.date}</p></article>)}</div>:empty('No reviews','This customer has not reviewed any product.'))}{tab==='activity'&&<div className="space-y-8"><section><h2 className="flex items-center gap-2 font-black"><Heart className="size-4 text-rose-500"/>Wishlist ({stats.wishlist})</h2><div className="mt-4"><ProductActivity items={wishlist} icon={Heart} emptyTitle="Wishlist is empty"/></div></section><section><h2 className="flex items-center gap-2 font-black"><Eye className="size-4 text-blue-500"/>Recently viewed ({stats.recentlyViewed})</h2><div className="mt-4"><ProductActivity items={recentlyViewed} icon={Eye} emptyTitle="No recently viewed products"/></div></section></div>}</div></section></div></AdminLayout>}