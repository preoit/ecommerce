import { Head, router } from '@inertiajs/react';
import { BadgeCheck, CalendarDays, CheckCircle2, Mail, MapPin, PackageCheck, Search, ShieldQuestion, ShoppingBag, UserRound, UsersRound, WalletCards } from 'lucide-react';
import { useEffect, useState } from 'react';
import AdminLayout from '@/app/layouts/AdminLayout';
import Seo from '@/app/components/Seo';

const money = value => `BDT ${Number(value || 0).toLocaleString('en-BD', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const initials = name => name?.split(' ').slice(0, 2).map(part => part[0]).join('').toUpperCase() || 'C';

function Verification({ verified, label }) {
    const Icon = verified ? BadgeCheck : ShieldQuestion;
    return <span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[11px] font-bold ${verified ? 'bg-emerald-50 text-emerald-700 ring-1 ring-emerald-200' : 'bg-amber-50 text-amber-700 ring-1 ring-amber-200'}`}><Icon className="size-3.5" />{label} {verified ? 'verified' : 'pending'}</span>;
}

function StatCard({ icon: Icon, label, value, tone }) {
    return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_24px_rgba(30,41,59,.06)]"><div className="flex items-center justify-between gap-4"><div><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 text-2xl font-black tracking-tight text-slate-950">{value}</p></div><span className={`grid size-12 place-items-center rounded-2xl ${tone}`}><Icon className="size-5" /></span></div></article>;
}

function CustomerCard({ customer }) {
    return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-black text-white">{initials(customer.name)}</span><div className="min-w-0 flex-1"><h3 className="truncate font-bold text-slate-950">{customer.name}</h3><p className="text-xs text-slate-400">Customer #{customer.id}</p></div></div><div className="mt-4 space-y-2 text-sm text-slate-600"><p className="flex items-center gap-2"><Mail className="size-4 text-slate-400"/><span className="truncate">{customer.email || 'No email added'}</span></p><p className="flex items-center gap-2"><UserRound className="size-4 text-slate-400"/>{customer.phone || 'No phone added'}</p></div><div className="mt-3 flex flex-wrap gap-2"><Verification verified={customer.emailVerified} label="Email"/><Verification verified={customer.phoneVerified} label="Phone"/></div><dl className="mt-4 grid grid-cols-3 gap-2 border-t border-slate-100 pt-4 text-center"><div><dt className="text-[11px] text-slate-400">Orders</dt><dd className="font-black">{customer.orders}</dd></div><div><dt className="text-[11px] text-slate-400">Addresses</dt><dd className="font-black">{customer.addresses}</dd></div><div><dt className="text-[11px] text-slate-400">Spent</dt><dd className="text-xs font-black text-violet-700">{money(customer.spent)}</dd></div></dl><p className="mt-4 text-xs text-slate-400">Registered {customer.registeredAt}</p></article>;
}

export default function Index({ customers, filters, stats }) {
    const [search, setSearch] = useState(filters.search || '');
    useEffect(() => { const timer = setTimeout(() => { if (search !== (filters.search || '')) router.get(route('customers.index'), search ? { search } : {}, { preserveState: true, replace: true }); }, 350); return () => clearTimeout(timer); }, [search]);
    return <AdminLayout><Head title="Customers"/><Seo title="Customers" description="Registered customer accounts, activity and order value."/><div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold text-violet-600">Customer management</p><h1 className="mt-1 text-3xl font-black tracking-tight text-slate-950">Registered customers</h1><p className="mt-2 text-sm text-slate-500">Customer identity, verification and purchase activity in one place.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-xl border border-violet-200 bg-violet-50 px-4 py-2 text-sm font-bold text-violet-700"><UsersRound className="size-4"/>{stats.registered} total customers</span></header><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={UsersRound} label="Registered customers" value={stats.registered} tone="bg-violet-50 text-violet-700"/><StatCard icon={CheckCircle2} label="Contact verified" value={stats.verified} tone="bg-emerald-50 text-emerald-700"/><StatCard icon={ShoppingBag} label="Customers with orders" value={stats.withOrders} tone="bg-blue-50 text-blue-700"/><StatCard icon={WalletCards} label="Completed sales" value={money(stats.totalSpent)} tone="bg-amber-50 text-amber-700"/></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_12px_32px_rgba(30,41,59,.07)]"><div className="flex flex-col gap-3 border-b border-slate-200 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-black text-slate-950">Customer directory</h2><p className="text-sm text-slate-500">Only storefront customer registrations are shown.</p></div><label className="relative block w-full sm:max-w-sm"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input value={search} onChange={event => setSearch(event.target.value)} placeholder="Search name, phone or email" className="h-11 w-full rounded-xl border-slate-200 bg-slate-50 pl-10 pr-4 text-sm focus:border-violet-500 focus:ring-violet-500"/></label></div>{customers.data.length ? <><div className="grid gap-4 p-4 md:hidden">{customers.data.map(customer => <CustomerCard key={customer.id} customer={customer}/>)}</div><div className="hidden overflow-x-auto md:block"><table className="min-w-[1100px] w-full"><thead className="bg-slate-50 text-left text-xs font-bold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Contact & verification</th><th className="px-5 py-4 text-center">Orders</th><th className="px-5 py-4 text-center">Addresses</th><th className="px-5 py-4">Total spent</th><th className="px-5 py-4">Registered / last order</th></tr></thead><tbody className="divide-y divide-slate-100">{customers.data.map(customer => <tr key={customer.id} className="transition hover:bg-violet-50/40"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-black text-white shadow-sm">{initials(customer.name)}</span><div><b className="block text-sm text-slate-950">{customer.name}</b><span className="text-xs text-slate-400">Customer #{customer.id}</span></div></div></td><td className="px-5 py-4"><p className="max-w-64 truncate text-sm font-semibold text-slate-700">{customer.email || 'No email added'}</p><p className="mt-1 text-xs text-slate-500">{customer.phone || 'No phone added'}</p><div className="mt-2 flex flex-wrap gap-1.5"><Verification verified={customer.emailVerified} label="Email"/><Verification verified={customer.phoneVerified} label="Phone"/></div></td><td className="px-5 py-4 text-center"><b className="text-lg text-slate-950">{customer.orders}</b><p className="text-xs text-emerald-600">{customer.completedOrders} completed</p></td><td className="px-5 py-4 text-center"><span className="inline-flex items-center gap-1.5 rounded-lg bg-slate-100 px-3 py-2 text-sm font-bold text-slate-700"><MapPin className="size-4"/>{customer.addresses}</span></td><td className="px-5 py-4"><b className="text-sm text-violet-700">{money(customer.spent)}</b></td><td className="px-5 py-4 text-xs text-slate-500"><p className="flex items-center gap-1.5"><CalendarDays className="size-3.5"/>{customer.registeredAt}</p><p className="mt-2 flex items-center gap-1.5"><PackageCheck className="size-3.5"/>{customer.lastOrderAt || 'No orders yet'}</p></td></tr>)}</tbody></table></div><div className="flex flex-wrap items-center justify-between gap-3 border-t border-slate-200 px-5 py-4"><p className="text-sm text-slate-500">Showing {customers.from}–{customers.to} of {customers.total}</p><nav className="flex flex-wrap gap-1">{customers.links.map((link,index) => <button key={index} disabled={!link.url} onClick={() => link.url && router.visit(link.url, { preserveState: true })} className={`min-w-9 rounded-lg border px-3 py-2 text-xs font-bold ${link.active ? 'border-violet-600 bg-violet-600 text-white' : 'border-slate-200 bg-white text-slate-600 hover:border-violet-300'} disabled:cursor-not-allowed disabled:opacity-40`} dangerouslySetInnerHTML={{ __html: link.label }}/>)}</nav></div></> : <div className="grid min-h-80 place-items-center p-8 text-center"><div><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-violet-50 text-violet-600"><UsersRound className="size-6"/></span><h3 className="mt-4 text-lg font-black">{search ? 'No matching customers' : 'No customers registered yet'}</h3><p className="mt-2 text-sm text-slate-500">{search ? 'Try another name, phone number or email.' : 'New customer registrations will appear here automatically.'}</p></div></div>}</section></div></AdminLayout>;
}