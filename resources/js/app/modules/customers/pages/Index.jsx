import { Head, Link, router, useForm } from '@inertiajs/react';
import { BadgeCheck, CalendarDays, CheckCircle2, Eye, Mail, MapPin, PackageCheck, Pencil, Search, ShieldQuestion, ShoppingBag, Trash2, UserRound, UsersRound, WalletCards, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import AdminLayout from '@/app/layouts/AdminLayout';
import Seo from '@/app/components/Seo';

const money=value=>`BDT ${Number(value||0).toLocaleString('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const initials=name=>name?.split(' ').slice(0,2).map(part=>part[0]).join('').toUpperCase()||'C';
function Verification({verified,label}){const Icon=verified?BadgeCheck:ShieldQuestion;return <span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[11px] font-bold ${verified?'bg-emerald-50 text-emerald-700 ring-1 ring-emerald-200':'bg-amber-50 text-amber-700 ring-1 ring-amber-200'}`}><Icon className="size-3.5"/>{label} {verified?'verified':'pending'}</span>}
function StatCard({icon:Icon,label,value,tone}){return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_24px_rgba(30,41,59,.06)]"><div className="flex items-center justify-between gap-4"><div><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 text-2xl font-black tracking-tight text-slate-950">{value}</p></div><span className={`grid size-12 place-items-center rounded-2xl ${tone}`}><Icon className="size-5"/></span></div></article>}
function Actions({customer,onEdit,onDelete}){return <div className="flex items-center justify-end gap-1.5"><Link href={route('customers.show',customer.id)} className="grid size-9 place-items-center rounded-xl bg-blue-50 text-blue-600 ring-1 ring-blue-100 transition hover:bg-blue-600 hover:text-white" title="View customer" aria-label={`View ${customer.name}`}><Eye className="size-4"/></Link><button type="button" onClick={()=>onEdit(customer)} className="grid size-9 place-items-center rounded-xl bg-violet-50 text-violet-600 ring-1 ring-violet-100 transition hover:bg-violet-600 hover:text-white" title="Edit customer" aria-label={`Edit ${customer.name}`}><Pencil className="size-4"/></button><button type="button" disabled={!customer.canDelete} onClick={()=>onDelete(customer)} className="grid size-9 place-items-center rounded-xl bg-rose-50 text-rose-600 ring-1 ring-rose-100 transition hover:bg-rose-600 hover:text-white disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-300 disabled:ring-slate-200" title={customer.canDelete?'Delete customer':'Verified or purchased accounts cannot be deleted'} aria-label={`Delete ${customer.name}`}><Trash2 className="size-4"/></button></div>}
function EditModal({customer,onClose}){const form=useForm({name:customer.name||'',email:customer.email||'',phone:customer.phone||''});const submit=e=>{e.preventDefault();form.patch(route('customers.update',customer.id),{preserveScroll:true,onSuccess:onClose})};useEffect(()=>{const close=e=>e.key==='Escape'&&onClose();document.addEventListener('keydown',close);return()=>document.removeEventListener('keydown',close)},[]);return <div className="fixed inset-0 z-[100] grid place-items-center p-4"><button onClick={onClose} className="absolute inset-0 bg-slate-950/50 backdrop-blur-sm" aria-label="Close edit modal"/><section role="dialog" aria-modal="true" aria-labelledby="edit-customer-title" className="relative w-full max-w-xl overflow-hidden rounded-2xl bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-200 px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-wider text-violet-600">Customer #{customer.id}</p><h2 id="edit-customer-title" className="mt-1 text-xl font-black">Edit customer</h2><p className="mt-1 text-sm text-slate-500">Changing email or phone resets its verification.</p></div><button type="button" onClick={onClose} className="grid size-9 place-items-center rounded-full bg-slate-100 text-slate-500 hover:bg-rose-50 hover:text-rose-600"><X className="size-4"/></button></header><form onSubmit={submit} className="space-y-5 p-6">{[['name','Full name','text'],['email','Email address','email'],['phone','Phone number','tel']].map(([key,label,type])=><label key={key} className="block text-sm font-bold text-slate-700">{label}<input type={type} value={form.data[key]} onChange={e=>form.setData(key,e.target.value)} required={key!=='email'} className="mt-2 h-12 w-full rounded-xl border-slate-300 bg-slate-50 focus:border-violet-500 focus:bg-white focus:ring-violet-500"/>{form.errors[key]&&<small className="mt-1.5 block font-semibold text-rose-600">{form.errors[key]}</small>}</label>)}<div className="flex justify-end gap-3 border-t border-slate-100 pt-5"><button type="button" onClick={onClose} className="h-11 rounded-xl border border-slate-300 px-5 text-sm font-bold text-slate-600">Cancel</button><button disabled={form.processing} className="h-11 rounded-xl bg-gradient-to-r from-violet-600 to-indigo-600 px-6 text-sm font-bold text-white shadow-lg shadow-violet-200 disabled:opacity-50">{form.processing?'Saving...':'Save changes'}</button></div></form></section></div>}
function CustomerCard({customer,onEdit,onDelete}){return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-black text-white">{initials(customer.name)}</span><div className="min-w-0 flex-1"><h3 className="truncate font-bold">{customer.name}</h3><p className="text-xs text-slate-400">Customer #{customer.id}</p></div><Actions customer={customer} onEdit={onEdit} onDelete={onDelete}/></div><div className="mt-4 space-y-2 text-sm text-slate-600"><p className="flex items-center gap-2"><Mail className="size-4 text-slate-400"/><span className="truncate">{customer.email||'No email added'}</span></p><p className="flex items-center gap-2"><UserRound className="size-4 text-slate-400"/>{customer.phone||'No phone added'}</p></div><div className="mt-3 flex flex-wrap gap-2"><Verification verified={customer.emailVerified} label="Email"/><Verification verified={customer.phoneVerified} label="Phone"/></div><dl className="mt-4 grid grid-cols-3 gap-2 border-t pt-4 text-center"><div><dt className="text-[11px] text-slate-400">Orders</dt><dd className="font-black">{customer.orders}</dd></div><div><dt className="text-[11px] text-slate-400">Addresses</dt><dd className="font-black">{customer.addresses}</dd></div><div><dt className="text-[11px] text-slate-400">Spent</dt><dd className="text-xs font-black text-violet-700">{money(customer.spent)}</dd></div></dl></article>}
export default function Index({customers,filters,stats}){const[search,setSearch]=useState(filters.search||'');const[editing,setEditing]=useState(null);const[deleteError,setDeleteError]=useState('');useEffect(()=>{const timer=setTimeout(()=>{if(search!==(filters.search||''))router.get(route('customers.index'),search?{search}:{},{preserveState:true,replace:true})},350);return()=>clearTimeout(timer)},[search]);const remove=customer=>{if(!customer.canDelete)return;if(!window.confirm(`Delete ${customer.name}? This cannot be undone.`))return;setDeleteError('');router.delete(route('customers.destroy',customer.id),{preserveScroll:true,onError:errors=>setDeleteError(errors.customer||'Customer could not be deleted.')})};return <AdminLayout><Head title="Customers"/><Seo title="Customers" description="Registered customer accounts, activity and order value."/><div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold text-violet-600">Customer management</p><h1 className="mt-1 text-3xl font-black">Registered customers</h1><p className="mt-2 text-sm text-slate-500">Customer identity, verification and purchase activity in one place.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-xl border border-violet-200 bg-violet-50 px-4 py-2 text-sm font-bold text-violet-700"><UsersRound className="size-4"/>{stats.registered} total customers</span></header>{deleteError&&<p className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm font-bold text-rose-700">{deleteError}</p>}<section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={UsersRound} label="Registered customers" value={stats.registered} tone="bg-violet-50 text-violet-700"/><StatCard icon={CheckCircle2} label="Contact verified" value={stats.verified} tone="bg-emerald-50 text-emerald-700"/><StatCard icon={ShoppingBag} label="Customers with orders" value={stats.withOrders} tone="bg-blue-50 text-blue-700"/><StatCard icon={WalletCards} label="Completed sales" value={money(stats.totalSpent)} tone="bg-amber-50 text-amber-700"/></section><section className="overflow-hidden rounded-2xl border bg-white shadow-[0_12px_32px_rgba(30,41,59,.07)]"><div className="flex flex-col gap-3 border-b p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-black">Customer directory</h2><p className="text-sm text-slate-500">View activity, edit details or remove eligible accounts.</p></div><label className="relative block w-full sm:max-w-sm"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, phone or email" className="h-11 w-full rounded-xl border-slate-200 bg-slate-50 pl-10 text-sm focus:border-violet-500 focus:ring-violet-500"/></label></div>{customers.data.length?<><div className="grid gap-4 p-4 lg:hidden">{customers.data.map(c=><CustomerCard key={c.id} customer={c} onEdit={setEditing} onDelete={remove}/>)}</div><div className="hidden overflow-x-auto lg:block"><table className="w-full min-w-[1220px]"><thead className="bg-slate-50 text-left text-xs font-bold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Contact & verification</th><th className="px-5 py-4 text-center">Orders</th><th className="px-5 py-4 text-center">Addresses</th><th className="px-5 py-4">Total spent</th><th className="px-5 py-4">Registered / last order</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y">{customers.data.map(c=><tr key={c.id} className="hover:bg-violet-50/40"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-black text-white">{initials(c.name)}</span><div><b className="block text-sm">{c.name}</b><span className="text-xs text-slate-400">Customer #{c.id}</span></div></div></td><td className="px-5 py-4"><p className="max-w-64 truncate text-sm font-semibold">{c.email||'No email added'}</p><p className="mt-1 text-xs text-slate-500">{c.phone||'No phone added'}</p><div className="mt-2 flex gap-1.5"><Verification verified={c.emailVerified} label="Email"/><Verification verified={c.phoneVerified} label="Phone"/></div></td><td className="px-5 py-4 text-center"><b className="text-lg">{c.orders}</b><p className="text-xs text-emerald-600">{c.completedOrders} completed</p></td><td className="px-5 py-4 text-center"><span className="inline-flex items-center gap-1 rounded-lg bg-slate-100 px-3 py-2 font-bold"><MapPin className="size-4"/>{c.addresses}</span></td><td className="px-5 py-4 text-sm font-bold text-violet-700">{money(c.spent)}</td><td className="px-5 py-4 text-xs text-slate-500"><p className="flex gap-1.5"><CalendarDays className="size-3.5"/>{c.registeredAt}</p><p className="mt-2 flex gap-1.5"><PackageCheck className="size-3.5"/>{c.lastOrderAt||'No orders yet'}</p></td><td className="px-5 py-4"><Actions customer={c} onEdit={setEditing} onDelete={remove}/></td></tr>)}</tbody></table></div><div className="flex flex-wrap items-center justify-between gap-3 border-t px-5 py-4"><p className="text-sm text-slate-500">Showing {customers.from}–{customers.to} of {customers.total}</p><nav className="flex gap-1">{customers.links.map((link,i)=><button key={i} disabled={!link.url} onClick={()=>link.url&&router.visit(link.url,{preserveState:true})} className={`min-w-9 rounded-lg border px-3 py-2 text-xs font-bold ${link.active?'border-violet-600 bg-violet-600 text-white':'border-slate-200 text-slate-600'} disabled:opacity-40`} dangerouslySetInnerHTML={{__html:link.label}}/>)}</nav></div></>:<div className="grid min-h-80 place-items-center p-8 text-center"><div><UsersRound className="mx-auto size-10 text-violet-500"/><h3 className="mt-4 text-lg font-black">{search?'No matching customers':'No customers registered yet'}</h3></div></div>}</section></div>{editing&&<EditModal customer={editing} onClose={()=>setEditing(null)}/>}</AdminLayout>}