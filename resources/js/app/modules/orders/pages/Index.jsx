import { Head, Link } from '@inertiajs/react';
import { CalendarDays, ChevronDown, Download, Eye, Filter, ListFilter, Mail, Pencil, Phone, Plus, Search, Trash2 } from 'lucide-react';
import { useMemo, useState } from 'react';
import AdminLayout from '@/app/layouts/AdminLayout';

const money = value => 'BDT ' + Number(value || 0).toLocaleString('en-BD', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
const statuses = ['Pending', 'Confirmed', 'Processing', 'Ready to Ship', 'Shipped', 'Completed', 'Cancelled', 'Returned'];
const statusStyle = { Pending: 'border-amber-200 bg-amber-50 text-amber-700', Confirmed: 'border-cyan-200 bg-cyan-50 text-cyan-700', Processing: 'border-sky-200 bg-sky-50 text-sky-700', 'Ready to Ship': 'border-violet-200 bg-violet-50 text-violet-700', Shipped: 'border-blue-200 bg-blue-50 text-blue-700', Completed: 'border-emerald-200 bg-emerald-50 text-emerald-700', Cancelled: 'border-rose-200 bg-rose-50 text-rose-700', Returned: 'border-orange-200 bg-orange-50 text-orange-700' };

export default function OrdersIndex({ orders = [] }) {
    const [query, setQuery] = useState('');
    const [status, setStatus] = useState('All Status');
    const [date, setDate] = useState('');
    const filtered = useMemo(() => orders.filter(order => `${order.number} ${order.customer} ${order.email}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All Status' || order.status === status) && (!date || order.date.includes(date))), [orders, query, status, date]);
    const count = item => orders.filter(order => order.status === item).length;

    return <AdminLayout><Head title="Orders" /><main className="-mx-4 -my-6 min-h-screen bg-[#f8f7fa] p-4 text-slate-800 dark:bg-slate-950 dark:text-slate-100 sm:-mx-6 sm:p-6 lg:-mx-8 lg:-my-8 lg:p-8"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-col justify-between gap-4 px-6 py-5 sm:flex-row sm:items-center"><div><h1 className="text-lg font-bold">Orders Management</h1><p className="mt-1 text-sm text-slate-500">Manage and track all orders</p></div><button className="inline-flex items-center gap-2 rounded-xl bg-gradient-to-r from-sky-500 to-teal-400 px-5 py-2.5 text-sm font-semibold text-white"><Plus className="size-4" />New Order</button></div><div className="grid gap-3 px-6 pb-6 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8">{statuses.map(item => <button key={item} onClick={() => setStatus(status === item ? 'All Status' : item)} className={`flex items-center justify-between rounded-xl border px-3 py-3 text-left text-xs font-semibold ${statusStyle[item]} ${status === item ? 'ring-2 ring-offset-1 ring-violet-500' : ''}`}><span>{item}</span><b className="text-xl">{count(item)}</b></button>)}</div><div className="grid gap-3 border-y border-slate-200 bg-slate-50/80 p-5 lg:grid-cols-[minmax(0,1fr)_190px_190px_auto]"><label className="relative"><Search className="pointer-events-none absolute left-3 top-3 size-4 text-slate-400" /><input value={query} onChange={event => setQuery(event.target.value)} className="h-11 w-full rounded-xl border-slate-200 bg-white pl-10 text-sm" placeholder="Search by order ID or customer..." /></label><label className="relative"><CalendarDays className="pointer-events-none absolute right-3 top-3 size-4 text-sky-500" /><input type="date" value={date} onChange={event => setDate(event.target.value)} className="h-11 w-full rounded-xl border-slate-200 bg-white px-3 text-sm" /></label><label className="relative"><select value={status} onChange={event => setStatus(event.target.value)} className="h-11 w-full appearance-none rounded-xl border-slate-200 bg-white px-3 text-sm"><option>All Status</option>{statuses.map(item => <option key={item}>{item}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-3 size-4" /></label><div className="flex gap-2"><button className="grid size-11 place-items-center rounded-xl border bg-white"><Filter className="size-4" /></button><button className="grid size-11 place-items-center rounded-xl border bg-white"><Download className="size-4" /></button><button className="grid size-11 place-items-center rounded-xl border bg-white text-sky-600"><ListFilter className="size-4" /></button></div></div><div className="overflow-x-auto"><table className="min-w-[1080px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs font-bold uppercase tracking-wide text-slate-500"><tr>{['Order', 'Customer', 'Products', 'Delivery', 'Order note', 'Amount', 'Status', 'Date', 'Actions'].map(heading => <th key={heading} className="px-5 py-4">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-200">{filtered.map(order => <tr key={order.id || order.number} className={`align-top transition-colors hover:bg-violet-50/50 ${!order.viewed ? 'bg-violet-50/80 ring-1 ring-inset ring-violet-100' : 'bg-white dark:bg-slate-900'}`}><td className="px-5 py-5 font-semibold text-violet-600"><Link href={route('orders.show', order.id)} className="hover:underline">{order.number}</Link>{!order.viewed && <span className="mt-2 block w-fit rounded-full bg-violet-600 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide text-white">New</span>}</td><td className="px-5 py-5"><b className="block">{order.customer}</b><small className="mt-1 flex items-center gap-1.5 text-slate-500"><Phone className="size-3.5" />{order.phone}</small><small className="mt-1 flex items-center gap-1.5 text-slate-400"><Mail className="size-3.5" />{order.email}</small></td><td className="max-w-[180px] px-5 py-5 leading-5">{order.product}</td><td className="max-w-[180px] px-5 py-5 text-slate-500">{order.delivery}</td><td className="max-w-[150px] px-5 py-5 text-slate-500">{order.note || '—'}</td><td className="min-w-[180px] px-5 py-5"><div className="space-y-1.5 text-xs"><div className="flex items-center justify-between gap-4 text-slate-500"><span>Products</span><b className="text-slate-700">{money(order.subtotal)}</b></div><div className="flex items-center justify-between gap-4 text-slate-500"><span>Delivery</span>{Number(order.shipping)>0?<b className="text-slate-700">{money(order.shipping)}</b>:<b className="rounded-full bg-emerald-50 px-2 py-0.5 text-emerald-700">Free</b>}</div>{Number(order.codSurcharge)>0&&<div className="flex items-center justify-between gap-4 text-slate-500"><span>COD charge</span><b className="text-slate-700">{money(order.codSurcharge)}</b></div>}<div className="mt-2 flex items-center justify-between gap-4 border-t border-slate-200 pt-2 text-sm"><strong>Total</strong><strong className="text-violet-700">{money(order.total)}</strong></div></div></td><td className="px-5 py-5"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${statusStyle[order.status]}`}>{order.status}</span></td><td className="px-5 py-5 text-slate-500">{order.date}<small className="mt-1 block">{order.time}</small></td><td className="px-5 py-5"><div className="flex gap-2"><Link href={route('orders.show', order.id)} title="View order" aria-label={`View ${order.number}`} className={`grid size-8 place-items-center rounded-full ${!order.viewed ? 'bg-violet-600 text-white' : 'bg-slate-100 text-slate-600 hover:bg-violet-100 hover:text-violet-700'}`}><Eye className="size-4" /></Link><button className="grid size-8 place-items-center rounded-full bg-slate-100 text-slate-600"><Pencil className="size-4" /></button><button className="grid size-8 place-items-center rounded-full bg-slate-100 text-slate-600"><Trash2 className="size-4" /></button></div></td></tr>)}{filtered.length === 0 && <tr><td colSpan="9" className="px-5 py-14 text-center text-slate-500">No orders match your filters.</td></tr>}</tbody></table></div></section></main></AdminLayout>;
}
