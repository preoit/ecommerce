import { Head, useForm } from '@inertiajs/react';
import { PanelsTopLeft, Plus, Save, Trash2 } from 'lucide-react';
import { useState } from 'react';
import MediaLibraryPicker from '@/app/components/MediaLibraryPicker';
import RichTextEditor from '@/app/design-system/components/RichTextEditor';
import AdminLayout from '@/app/layouts/AdminLayout';
import Button from '@/app/design-system/components/Button';

const networks = ['Facebook', 'X', 'Instagram', 'YouTube', 'TikTok', 'WhatsApp'];
const Field = ({ label, value, change, placeholder = '' }) => <label className="block text-sm font-semibold text-slate-700">{label}<input value={value || ''} onChange={event => change(event.target.value)} placeholder={placeholder} className="mt-2 h-10 w-full rounded-lg border-slate-300 text-sm" /></label>;

export default function FooterSectionEdit({ footer, media = [] }) {
    const { data, setData, patch, processing } = useForm(footer);
    const [paymentImages, setPaymentImages] = useState(media.filter(file => (footer.payment_images || []).includes(file.path)));
    const groups = data.link_groups || []; const socials = data.social_links || {};
    const group = (index, value) => setData('link_groups', groups.map((item, current) => current === index ? value : item));
    const submit = event => { event.preventDefault(); patch('/admin/website-design/footer'); };
    return <AdminLayout><Head title="Footer" /><form onSubmit={submit} className="mx-auto max-w-6xl space-y-6"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-violet-100 text-violet-700"><PanelsTopLeft className="size-5" /></span><div><p className="text-sm text-violet-600">Website Design</p><h1 className="text-2xl font-bold">Footer</h1></div></div><section className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">Brand & contact</h2><div className="mt-5 grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Footer description<textarea value={data.description || ''} onChange={event => setData('description', event.target.value)} rows="4" className="mt-2 w-full rounded-lg border-slate-300" /></label><div className="space-y-5"><Field label="Phone number" value={data.phone} change={value => setData('phone', value)} /><Field label="Copyright name" value={data.copyright_name} change={value => setData('copyright_name', value)} /></div><Field label="App Store URL" value={data.app_store_url} change={value => setData('app_store_url', value)} /><Field label="Google Play URL" value={data.google_play_url} change={value => setData('google_play_url', value)} /></div></section><section className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">Footer link sections</h2><p className="mt-1 text-sm text-slate-500">Each title supports rich text, lists and hyperlinks.</p><div className="mt-5 space-y-5">{groups.map((item, index) => <article key={index} className="rounded-xl border border-slate-200 p-5"><div className="flex gap-3"><input value={item.title || ''} onChange={event => group(index, { ...item, title: event.target.value })} className="flex-1 rounded-lg border-slate-300 font-semibold" /><button type="button" onClick={() => setData('link_groups', groups.filter((_, current) => current !== index))} className="text-rose-600"><Trash2 className="size-4" /></button></div><div className="mt-4"><RichTextEditor id={`footer-section-${index}`} value={item.content || ''} onChange={content => group(index, { ...item, content })} placeholder="Add text, lists, or links…" /></div></article>)}</div><button type="button" onClick={() => setData('link_groups', [...groups, { title: 'New section', links: [], content: '' }])} className="mt-5 inline-flex items-center gap-2 rounded-lg border px-3 py-2 text-sm font-semibold text-violet-700"><Plus className="size-4" />Add section</button></section><section className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-bold">Social & payment methods</h2><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{networks.map(name => <Field key={name} label={`${name} URL`} value={socials[name]} change={value => setData('social_links', { ...socials, [name]: value })} placeholder={`https://${name.toLowerCase()}.com/...`} />)}</div><div className="mt-6 grid gap-6 md:grid-cols-2"><Field label="Payment methods" value={(data.payment_methods || []).join(', ')} change={value => setData('payment_methods', value.split(',').map(item => item.trim()).filter(Boolean))} /><div><p className="text-sm font-semibold">Payment logo images</p><div className="mt-2"><MediaLibraryPicker value={paymentImages} multiple onChange={files => { setPaymentImages(files); setData('payment_images', files.map(file => file.path)); }} label="Choose payment logos" /></div></div></div></section><div className="flex justify-end"><Button type="submit" icon={Save} loading={processing} className="!border-violet-600 !bg-violet-600">Save footer</Button></div></form></AdminLayout>;
}
